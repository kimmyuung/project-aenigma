# =============================================================================
# AI 기반 감성 일기 앱 - 프로덕션 환경 변수
# =============================================================================
# ⚠️ 경고: 이 파일을 .env.production으로 복사하고 실제 값을 입력하세요.
#         절대로 Git에 커밋하지 마세요!
# =============================================================================

# Django 설정
DEBUG=False
SECRET_KEY=your-super-secret-key-change-this-in-production
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,your-ec2-ip

# 데이터베이스 (PostgreSQL)
POSTGRES_DB=diary_db
POSTGRES_USER=diary_user
POSTGRES_PASSWORD=your-strong-database-password

# Redis (Celery 비동기 처리용)
REDIS_URL=redis://redis:6379/0

# OpenAI API (AI 기능)
OPENAI_API_KEY=sk-your-openai-api-key

# 일기 암호화 키 (Fernet 32바이트 키)
# 생성: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ⚠️ 이 키를 분실하면 기존 일기 복구 불가능!
DIARY_ENCRYPTION_KEY=your-fernet-encryption-key

# CORS 설정 (쉼표로 구분)
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# AWS S3 (선택사항 - 미디어 파일 저장)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=ap-northeast-2

# 이메일 설정 (선택사항)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-email-app-password

# Sentry 에러 추적 (선택사항)
# SENTRY_DSN=https://your-sentry-dsn
